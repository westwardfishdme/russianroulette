# russianroulette
A simple Russian Roulette game written in Rust.
Built this to mess around with STDIN functionality in std::io.

# Building
`cargo build --release` --> binary is build to `target/release/Russian_Roulette`
